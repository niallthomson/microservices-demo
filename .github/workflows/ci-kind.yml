name: Kind CI

on:
  push:
    branches:
    - master

jobs:
  build:
    name: CI
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v2
    - name: Build images
      env:
        REPO: watchn
        TAG: ${GITHUB_SHA}
      run: |
        scripts/build-all.sh
    - uses: engineerd/setup-kind@v0.5.0
    - name: Setup helmfile
      uses: mamezou-tech/setup-helmfile@v0.7.0
    - name: Deploy
      env:
        IMAGE_TAG: ${GITHUB_SHA}
      run: |
        cd deploy/kubernetes && helmfile apply
        kubectl get pod -A
    