#@ load("@ytt:data", "data")

#@ if not len(data.values.imageTag) > 0:
---
apiVersion: kbld.k14s.io/v1alpha1
kind: Sources
sources:
- image: #@ data.values.frontend.image
  path: #@ data.values.frontend.imagePath

#@ if/end len(data.values.pushRepo) > 0:
---
apiVersion: kbld.k14s.io/v1alpha1
kind: ImageDestinations
destinations:
- image: #@ data.values.frontend.image
  newImage: #@ data.values.pushRepo + '/shop-front-end'
#@ end

#@ if/end len(data.values.imageTag) > 0:
---
apiVersion: kbld.k14s.io/v1alpha1
kind: ImageOverrides
overrides:
- image: #@ data.values.frontend.image
  newImage: #@ 'docker.io/niallthomson/shop-front-end:' + data.values.imageTag
  preresolved: true