#@ load("@ytt:data", "data")

#@ if data.values.ui.metrics.enabled:
---
apiVersion: v1
kind: Service
metadata:
  name: ui-metrics
  namespace: #@ data.values.namespace
  annotations: #@ data.values.ui.metrics.service.annotations
spec:
  type: ClusterIP
  ports:
  - port: #@ data.values.ui.metrics.service.port
    targetPort: http
  selector:
    name: ui
#@ end