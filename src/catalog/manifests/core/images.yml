#@ load("@ytt:data", "data")

#@ if not len(data.values.imageTag) > 0:
---
apiVersion: kbld.k14s.io/v1alpha1
kind: Sources
sources:
- image: #@ data.values.catalog.image
  path: #@ data.values.catalog.imagePath

#@ if/end len(data.values.pushRepo) > 0:
---
apiVersion: kbld.k14s.io/v1alpha1
kind: ImageDestinations
destinations:
- image: #@ data.values.catalog.image
  newImage: #@ data.values.pushRepo + '/shop-catalog'
#@ end

#@ if/end len(data.values.imageTag) > 0:
---
apiVersion: kbld.k14s.io/v1alpha1
kind: ImageOverrides
overrides:
- image: #@ data.values.catalog.image
  newImage: #@ 'docker.io/niallthomson/shop-catalog:' + data.values.imageTag
  preresolved: true