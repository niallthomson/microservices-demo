#@ load("@ytt:data", "data")

#@ if data.values.catalog.hpa.enabled:
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: catalogue
  namespace: #@ data.values.namespace
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: catalogue
  minReplicas: #@ data.values.catalog.hpa.minReplicas
  maxReplicas: #@ data.values.catalog.hpa.maxReplicas
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
#@ end