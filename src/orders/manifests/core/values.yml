#@data/values
---
#@overlay/match missing_ok=True
orders:
  image: 
    name: watchn-orders
    build:
      id: watchn-orders
      path: .
  replicas: 1
  services:
    mysqlWriter:
      secret: orders-writer-db
    mysqlReader:
      secret: orders-reader-db