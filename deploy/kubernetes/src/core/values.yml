#@data/values
---
namespace: watchn

image:
  tag: ""
  repository: "docker.io/niallthomson"
  push: false
  build:
    path: ../../../images

services:
  enabled: true

hpa:
  enabled: false

metrics:
  prometheus:
    enabled: false

eks:
  fargate:
    enabled: false

includeServices: false